# Backend Environment Variables

# Port (Render will set this automatically)
PORT=5000

# Path to XML files folder
# For production (Render): use ./xmldata
# For local development: use absolute path to your local ServerFiles
XML_FOLDER_PATH=./xmldata
